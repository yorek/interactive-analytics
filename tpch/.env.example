# Example environment file for the TPC-H benchmark. Copy to .env and adjust.
CONNECTION_NAME=DEFAULT

# Benchmarks use IW_TPCH_BENCH with TPCH_SF<scale> (standard) and
# TPCH_SF<scale>_IT (interactive) unless overridden via CLI flags.
DEFAULT_SCALE=10
